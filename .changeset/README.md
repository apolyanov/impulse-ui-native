# Changesets

Run `pnpm changeset` whenever a package change should be released.

The public `@impulse-ui-native/*` packages are configured as one fixed release
group, so they keep the same version number.
