import { ComponentSize } from "@impulse-ui-native/theme";

export const ControlContainerHeight: Record<ComponentSize, number> = {
  small: 36,
  medium: 44,
  large: 52,
};

export const ControlFontSize: Record<ComponentSize, number> = {
  small: 14,
  medium: 16,
  large: 18,
};
